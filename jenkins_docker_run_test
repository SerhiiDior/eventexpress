#!groovy
// Run docker 
properties([disableConcurrentBuilds()])

pipeline { 
    agent any 
    options {
        skipStagesAfterUnstable()
        timestamps()
    }
    stages {
        stage('Test Api') { 
            steps { echo "========== run image svdior/pyslim =========="
                sh 'docker run -v directory:/workdir svdior/pyslim pytest --alluredir /workdir/Reports_Allure /Tests' 
            }
        }
        stage('Test'){
            steps {
                sh 'docker run -v directory:/workdir svdior/pyslim pytest --alluredir /workdir/Reports_Allure /Tests'
                 
            }
        }
        stage('Allure_Report') {
            steps {
                sh 'docker run -it --rm -p 4040:4040 -p 5050:5050 -e CHECK_RESULTS_EVERY_SECONDS=3 -e KEEP_HISTORY="TRUE" -v Allure:/app/allure-results frankescobar/allure-docker-service'
            }
        }
    }
}








